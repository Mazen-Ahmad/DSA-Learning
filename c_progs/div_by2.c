#include<stdio.h>
int main()
{
    int n;
    printf("enter a no.");
    scanf("%d",&n);
    if(n%2==0)
    {
        printf("no. is divisible by 2");
    }
    else
    {
    printf("not divisible");
}
}